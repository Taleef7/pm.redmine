<div style="margin-top: 1em;">
  <label>
    <input type="checkbox" id="semantic-search-toggle" />
    Enable Semantic Search (RASS)
  </label>
</div>
<script>
  // On page load, set checkbox state from cookie
  document.addEventListener('DOMContentLoaded', function() {
    var toggle = document.getElementById('semantic-search-toggle');
    if (!toggle) return;
    var semantic = document.cookie.replace(/(?:(?:^|.*;\s*)semantic_search\s*\=\s*([^;]*).*$)|^.*$/, "$1");
    toggle.checked = semantic === '1';
    toggle.addEventListener('change', function() {
      document.cookie = 'semantic_search=' + (toggle.checked ? '1' : '0') + '; path=/';
      // Optionally, auto-submit the form if on the search page
      var form = document.getElementById('search-form');
      if (form) form.submit();
    });
  });
</script> 